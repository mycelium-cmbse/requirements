generic requiremt that GUI needs to reflect updates of visible items when they have been changed by the current user or another user.

SSS-PA-ARCH-TB2 -> prevent circular nesting
SSS-PA-ARCH-K7M -> prio?
SSS-PA-ARCH-N5W -> prio?
SSS-PT-DATA-D5I -> value switch, do we keep it?
SSS-PT-DATA-XHY -> is this one necessary, seems like a duplicute requirement
SSS-PA-ELEM-L9P -> check inheritance of relationships in the spec
SSS-PA-REQ-D7V -> reqif import -> using statements
SSS-PA-REQ-D7W -> reqif import -> using statements
SSS-PA-TRACE-F5M -> check for sensibility
SSS-FB-BEH-C7F -> check if this is the right location

resume at line XXX